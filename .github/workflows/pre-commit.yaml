name: pre-commit

on:
  pull_request:

jobs:
  pre-commit:
    uses: autowarefoundation/autoware-github-actions/.github/workflows/reusable-pre-commit.yaml@feef2b5395cf6de748cacc72f5ad08feea53e15a
    with:
      pre-commit-config: .pre-commit-config.yaml
